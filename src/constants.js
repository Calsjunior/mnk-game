const GAME_STATE = Object.freeze({
  PLAYING: "playing",
  WIN: "win",
  DRAW: "draw",
});

const SCREENS = Object.freeze({
  MENU: "menu",
  GAME: "game",
  SETTINGS: "settings",
});

export { GAME_STATE, SCREENS };
